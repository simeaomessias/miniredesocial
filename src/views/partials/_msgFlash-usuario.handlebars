{{#if msgSucesso}}
    <div class='container mt-3 text-center py-1' style="background-color: rgb(0,200,0); color: black; padding: 0px;">
        <span>{{msgSucesso}}</span>
    </div>
{{/if}}

{{#if msgErro}}
    <div class='container mt-3 text-center py-1' style="background-color: rgb(200,0,0); color: black; padding: 0px;">
        <span>{{msgErro}}</span>
    </div>
{{/if}}
